/**
 * 桌宠各状态的位置偏移配置
 *
 * 用途：工作态素材与「看鼠标（闲置）」素材的构图位置不一致，
 *       切换时猫会跳一下。这里给每段素材一个补偿偏移，让它们叠得上。
 *
 * 单位：百分比（相对画面尺寸）。
 *       ox: 左右（正 = 往右，负 = 往左）
 *       oy: 上下（正 = 往下，负 = 往上）
 *
 * ⚠️ 重要：这三段素材**出自同一个视频**，机位没动，
 *    所以猫在画面里的位置本来就是一致的 → **三段共用同一个偏移**即可。
 *    （不要按各自包围盒分别算：working 含有笔记本，包围盒会被撑大拉低，
 *     量出来的是「猫+笔记本」而不是猫，会得出错误的偏移。）
 *
 * 想微调：直接改下面的数字，保存后**重启桌宠**即可生效，不需要重新打包。
 */
window.PET_OFFSETS = {
  work_start: { ox: 4.6, oy: 5.7 },
  working:    { ox: 4.6, oy: 5.7 },
  work_done:  { ox: 4.6, oy: 5.7 }
};
