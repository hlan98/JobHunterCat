# CODE_PROVENANCE — 代码来源与许可边界

## 文件状态

本文件记录的是截至：

**2026-09-25**

的代码来源分析。

> 🔴 **2026-09-25 重大更正**：此前以「找工作喵 **2026-09-12** 完整包」为基线 —— **基线选错了**。
> 那个包本身已包含找工作喵自己早期的代码（`main.py` / `pet_bridge.py` / `desktop/`），
> 会把**自有早期代码误算成"来源于原项目"**，从而**严重低估**新增代码比例。
>
> 现在改用**上游原始包** `job-hunter-skill.zip`（顶层 `job_hunter_skill/` + `tests/` + `examples/`，
> 共 27 个文件）重新审计。结论发生**方向性变化**，见第三节。

后续代码变化**不自动继承**本文件中的分类。
每次重大代码来源变化后应**重新审计**。

> ⚠️ 本文件是**事实记录**，不是法律意见。
> 这里记录的是可核查的代码来源事实：哪些行来自哪里、改了多少。
> 它**不判断**许可在法律上如何适用。混合文件的授权问题涉及衍生作品认定、
> 贡献者权利、版权归属等具体法律问题，需要专业人士结合实际代码判断。
> 本文件的作用是：**把事实摊开，让任何评估者（包括律师）能自己核对。**

---

## 一、为什么需要这份文件

本项目起源于一个第三方 **MIT** 项目（`job-hunter-skill`），在其基础上做了修改，
并新增了桌宠前端等原创部分。

MIT 许可对相关代码授予了广泛的使用、修改和分发权利，并要求保留
原版权声明与许可声明。

因此，本项目后续新增代码所采用的限制性许可证，不应被表述为
对原 MIT 代码本身增加限制，也不应否定原 MIT 授权已经授予的权利。

> **表述提示**：不要把 MIT 的义务概括为"保留声明 + 不得追加限制"这样的两条清单。
> 更准确的表述是：**本项目不能通过自身后续许可证条款，否定或限制原 MIT 授权已经授予的权利。**

同时，本仓库中存在**混合文件** —— 同一个文件里既有 MIT 代码，也有后续新增代码。
必须把边界说清楚，让任何人能自行核对。

---

## 二、三类文件

| 类别 | 含义 | 本仓库对应 |
|---|---|---|
| **① 纯原创** | 完全由 JobHunter Cat 新增，不含任何原项目代码 | `agent/main.py`、`agent/pet_bridge.py`、`desktop/`、`memory/`、`assets/`、文档 |
| **② 来源于原项目** | 来自原项目，当前版本未发现后续修改 | `examples/` |
| **③ ★ 混合文件** | **原本是 MIT 代码，之后被修改/增删 —— 同一文件里既有 MIT 代码，也有后续新增代码** | `boss/`、`agent/shared.py`、`agent/ledger.py`、`agent/doctor.py`、`agent/skill_entry.py`、`tests/` |

### ★ 混合文件为什么是重点

以 `boss_apply.py` 为例：

```
boss_apply.py
│
├── 来自原项目的代码            ← 占绝大多数（相对基线 92% 未变）
│
└── 后续修改 / 新增的代码        ← 少数（相对基线新增 76 行、删除 34 行）
```

**不能**因为"我改了一部分"就把整个文件按新许可授权 ——
该文件仍**整体包含 MIT 代码**，而 MIT 要求这部分继续适用 MIT。

---

## 三、混合文件的实测数据

下面是**逐行差异实测**结果（方法见第五节）：

> ⚠️ 以下为 **2026-09-25 用上游原始包重新审计**后的数字（旧数字见文末「历史数据」）。

| 模块 | 基线行数 | 现有行数 | **未变行** | 新增行 | 删除行 | 未变占比 |
|---|---|---|---|---|---|---|
| `boss/boss_apply.py` | 433 | 975 | **394** | 581 | 39 | **40%** |
| `agent/shared.py` | 1,598 | 2,326 | **1,537** | 789 | 61 | **66%** |
| `agent/doctor.py` | 195 | 197 | **192** | 5 | 3 | **97%** |
| `agent/skill_entry.py` | 562 | 556 | **549** | 7 | 13 | **99%** |
| `tests/test_core.py` | 214 | 343 | **209** | 134 | 5 | **61%** |
| `examples/config.example.json` | 52 | 68 | **50** | 18 | 2 | **74%** |
| `examples/resume.example.md` | 41 | 41 | **41** | 0 | 0 | **100%** |
| `sxs_apply.py`（已删除） | 593 | — | — | — | 全部删除 | — |
| `__init__.py`（已删除） | 1 | — | — | — | 全部删除 | — |
| **合计（有对应的模块）** | **3,689** | **4,506** | **2,972** | **1,534** | **717** | **66.0%** |

### 🔴 上游原始包中**不存在**的文件（= 后续新增，不是混合文件）

| 文件 | 行数 | 说明 |
|---|---|---|
| `agent/main.py` | 7,296 | 桌宠主程序 |
| `boss/boss_chat.py` | 1,459 | HR 消息监听（**上游无此模块**）|
| `agent/pet_bridge.py` | 884 | Electron ↔ Python 桥接 |
| `desktop/`（全部） | 约 2,000 | Electron 桌宠前端 |
| `agent/ledger.py` | 106 | 动作账本（**上游无此模块**）|
| `tests/test_boss_chat.py` | 142 | 新增测试（**上游无此模块**）|
| 其余（文档 / `.github/` / `README_EN.md` 等） | 约 3,000 | — |
| **新增文件合计** | **13,992** | — |

### 结论（相对于**上游原始包**）

| | 行数 | 占比 |
|---|---|---|
| **继承自原项目（逐行未变）** | **2,972** | **16.1%** |
| **后续新增 / 重写** | **15,526** | **83.9%** |
| 现有可比代码总行 | 18,498 | 100% |

> 🔴 **与旧结论方向相反**：旧审计称「87.8% 未变、新增仅 12.2%」，
> 那是因为把找工作喵**自己的早期代码**当成了"原项目代码"。
> 相对**真正的上游原始包**，本项目 **83.9% 是后续新增/重写**，继承部分仅 **16.1%**。

⚠️ **但分类处置不变**：`boss_apply.py` / `shared.py` / `doctor.py` / `skill_entry.py` /
`tests/test_core.py` 这几个文件里**确实含有上游 MIT 代码**（未变行 394~1,537 行不等），
因此**仍按混合文件整体适用 MIT** —— 见第四节。

### 各文件的具体改动内容（供核对）

| 模块 | 主要改动 |
|---|---|
| `boss_apply.py` | 删除非 BOSS 平台分支；新增卡片级标题预过滤；新增 `_emit_progress` 进度事件 |
| `boss_chat.py` | 新增「要简历卡片」识别与自动发送链路；四类信号分类；分步诊断日志 |
| `shared.py` | 城市码表 11 → 63 个；移除表外城市静默回退；新增 LLM 客户端与评分 v2 |
| `ledger.py` | 新增 `resume_send` 动作类型 |
| `doctor.py` | 检查项微调 |
| `skill_entry.py` | 平台注册表缩减为仅 BOSS |
| `sxs_apply.py` | **整个文件已删除**（原为实习僧平台适配） |

---

## 四、分类结论

| 路径 | 类别 | 适用许可 |
|---|---|---|
| `boss/boss_apply.py` | ③ 混合 | **MIT** |
| `boss/boss_chat.py` | ③ 混合 | **MIT** |
| `agent/shared.py` | ③ 混合 | **MIT** |
| `agent/ledger.py` | ③ 混合 | **MIT** |
| `agent/doctor.py` | ③ 混合 | **MIT** |
| `agent/skill_entry.py` | ③ 混合 | **MIT** |
| `tests/test_core.py`、`tests/test_boss_chat.py` | ③ 混合 | **MIT** |
| `examples/config.example.json`、`examples/resume.example.md` | ② 来源于原项目，当前版本与基线快照**完全一致**（未发现后续修改） | **MIT** |
| `agent/main.py` | ① 纯原创 | PolyForm Noncommercial 1.0.0 |
| `agent/pet_bridge.py` | ① 纯原创 | PolyForm Noncommercial 1.0.0 |
| `desktop/`（全部） | ① 纯原创 | PolyForm Noncommercial 1.0.0 |
| `memory/`、`assets/`、`scripts/`、`docs/` | ① 纯原创 | PolyForm Noncommercial 1.0.0 |

**采用保守处置**：混合文件**整体按 MIT**，不对其追加任何限制。

理由（2026-09-25 更新）：

1. 混合文件中**确实含有上游 MIT 代码**（`boss_apply.py` 394 行、`shared.py` 1,537 行、
   `doctor.py` 192 行、`skill_entry.py` 549 行、`test_core.py` 209 行逐行未变）；
2. 无法逐行切分"哪一行属于谁"；
3. 因此整体按 MIT 处理最安全 —— 既满足 MIT 的保留要求，
   也避免误将 MIT 代码置于更严格的新许可之下。

> ⚠️ 注意：这里的理由**不再是**"原项目代码占绝大多数"。
> 相对上游原始包，这些文件里的继承部分约占 66%（`shared.py`）到 40%（`boss_apply.py`），
> **不是**旧审计所说的 87.8%。但**只要文件里含有 MIT 代码，整体按 MIT 就是正确处置** ——
> 这与占比高低无关。

> **补充说明**：`ledger.py`、`doctor.py`、`skill_entry.py` 相对基线的未变占比高达 97~98%，
> 说明它们同样主要来自基线快照，**不是**从零重写的模块。
> 因此不将其改判为"纯原创"。

> ⚠️ **关于"整体按 MIT"的限度**：这是本项目采取的**保守声明**，用于避免对 MIT 代码追加限制。
> 但混合文件能否"作为一个整体被商业使用/分发"，**不能仅凭本文件的一句话自动解决** ——
> 仍需结合具体代码由专业人士判断。本文件只负责把事实写清楚。

---

## 五、测量方法与局限

### 方法

1. 取**上游原始包**：`job-hunter-skill.zip`（2026-09-25 取得，共 27 个文件，
   结构为顶层 `job_hunter_skill/*.py` + `tests/` + `examples/`）。
   ✅ **这就是上游原始版本本身**，不再是"本项目某个早期快照"。
2. 对每个模块，用 `difflib.SequenceMatcher` 做**逐行匹配**
3. 「未变行」= 匹配块总行数；「新增行」= 现有行数 − 未变行；「删除行」= 基线行数 − 未变行
4. **上游查不到的模块**（如 `boss_chat.py` / `ledger.py` / `test_boss_chat.py`）
   单独归为「新增文件」，按行数列在第三节

### ⚠️ 局限（必须知道）

| 局限 | 说明 |
|---|---|
| 逐行匹配是近似 | 大段移动的代码可能被计为"新增+删除" |
| 上游包可能不完整 | 若上游还有本包未包含的文件/版本，测得的比例会略有偏差 |
| 「上游无对应」≠「无派生关系」 | `boss_chat.py` 等虽无对应文件，仍可能**借鉴**了上游写法。本文件只记录"无逐行对应"这一事实 |

### 与旧基线的差异（为什么数字变了这么多）

旧基线用的是「找工作喵 2026-09-12 完整包」—— 它已经包含本项目自己早期的
`main.py` / `pet_bridge.py` / `desktop/`。拿它当"原项目"比对，等于把
**自有早期代码算成了继承部分** → 87.8% 这个数字是虚高的。

上游原始包里**只有** skill 本体（`boss_apply` / `shared` / `doctor` / `skill_entry` /
`sxs_apply`），**没有**桌宠那一整套 —— 所以继承占比落到 16.1%，才符合实际。

---

## 六、如果你需要更强的依据

✅ **本节已于 2026-09-25 完成** —— 上游原始包已取得并完成逐行 diff，
结果已写入第三节。

遗留的一个**归类问题**（本文件不代为下结论）：

`boss/boss_chat.py`（1,459 行）、`agent/ledger.py`（106 行）、`tests/test_boss_chat.py`（142 行）
在**上游原始包中不存在**，逐行对应率为 0%。

- 若认定其为**纯原创** → 可归入 ①，适用 PolyForm Noncommercial；
- 但"无逐行对应"**不等于"无派生关系"**（仍可能借鉴了上游写法）。

**本项目当前采取保守处置：这 3 个文件仍留在 ③ 混合文件、整体按 MIT。**
理由：误将含 MIT 派生代码的文件置于更严格的许可下，风险高于"多给一些权利"。
若日后确认其确为独立原创，再由专业人士评估是否改判为 ①。

---

## 七、维护规则

1. 新增文件默认归 **① 纯原创**
2. **不要**把混合文件改判为纯原创，除非能证明其不含原项目代码
3. 每次改动混合文件后，**重跑第三节的差异分析**并更新数字
4. 引入新的第三方代码时，在此登记来源与许可
5. 许可边界变动时，同步更新 [`../NOTICE`](../NOTICE) 与 [`../README.md`](../README.md)
6. **每次重大代码来源变化后重新审计，并更新"文件状态"中的日期**

### 如何重跑差异分析

第三节的数字由 [`scripts/provenance_diff.py`](../scripts/provenance_diff.py) 生成：

```bash
# 普通表格
python scripts/provenance_diff.py --baseline <基线包.zip>

# 直接输出可粘贴进本文档的 Markdown 表格
python scripts/provenance_diff.py --baseline <基线包.zip> --markdown
```

基线包为**上游原始包** `job-hunter-skill.zip`（2026-09-25 取得，见第五节）。

> 🔴 **不要用本项目自己的早期完整包当基线**（2026-09-12 / 09-13 / 09-15 任一版本都不行）——
> 它们已包含本项目自有代码，会把自有代码算成"继承自原项目"，使新增比例被严重低估。
> 上游原始包只含 skill 本体（`job_hunter_skill/` + `tests/` + `examples/`），
> 可用「其中是否有 `main.py` / `pet_bridge.py` / `desktop/`」来快速判断基线对不对：
> **有 → 不是原始包**。

> ⚠️ 重跑后请**同时更新**：
> ① 第三节表格数字 ② 顶部「文件状态」中的日期 ③ 若分类有变，同步 NOTICE 与 README

---

## 附：第三方依赖许可（按实际锁定版本核验）

以下数据取自**本机实际安装版本**的包元数据（`importlib.metadata`），
而非依赖记忆或 PyPI 页面。

| 依赖 | 实测版本 | 许可（核验来源） |
|---|---|---|
| `DrissionPage` | 4.1.1.4 | ⚠️ **元数据未声明** —— 需人工核验其官方 LICENSE |
| `pillow` | 12.3.0 | MIT-CMU（`License-Expression` 字段） |
| `pypdf` | 6.17.0 | BSD-3-Clause（`License-Expression` 字段） |
| `pymupdf` | 1.28.0 | ⚠️ **Dual Licensed - GNU AFFERO GPL 3.0 or Artifex Commercial License** |
| `python-docx` | 1.2.0 | MIT License（Classifier 字段） |
| `rapidocr-onnxruntime` | 1.2.3 | Apache-2.0（Classifier 字段） |
| `onnxruntime` | 1.30.0 | MIT License（Classifier 字段） |
| `windnd` | 1.0.7 | MIT（包内 LICENSE 文件） |

### ⚠️ 商用前必读

1. **`pymupdf` 是 AGPL-3.0 或商业授权双轨** ——
   若你的商用场景需要闭源分发，AGPL 会带来源码开放义务。
   可选替代：`pypdf`（BSD-3-Clause，本项目已可回退使用）。
2. **`DrissionPage` 许可未能自动核验** —— 其包元数据未声明许可，
   需到其官方仓库确认 LICENSE 文件后再商用。
3. **本表按 2026-09-22 的锁定版本核验** ——
   升级依赖后需重新核验，许可证可能随版本变化。
4. 打包 EXE 分发前，建议把**所有依赖的 LICENSE 原文**收集到
   `LICENSES/` 或随包附带，以满足各许可的保留要求。
