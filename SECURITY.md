# Security Policy · 安全说明

本项目涉及**真实的账号、真实的简历、真实的 HR 消息**，因此安全问题比一般开源项目更需要说清楚。

---

## 支持的范围

| 版本 | 是否支持 |
|---|---|
| 主线（`main`） | ✅ |
| 历史 zip 发布包 | ❌ 不再维护，请升级到主线 |

---

## 如何报告漏洞

**请不要开公开 issue 讨论安全问题** —— 尤其是涉及可以被利用去**真实发送 HR 消息**的缺陷。

请通过私下方式联系作者：

- 邮箱：hlan98@hotmail.com
- QQ：`104495956`
- GitHub：<https://github.com/hlan98>（可先开一个不含细节的 issue 占位，再私聊细节）

请尽量包含：

1. 复现步骤
2. 影响范围（会不会导致真实消息被发出？会不会泄露本地数据？）
3. 你的环境（Windows 版本 / Python 版本 / Chrome 版本）

---

## ⚠️ 本项目特有的安全面

使用或二次开发前，请务必了解以下四点。

### 1. 它会真实发消息，且不可撤回

打招呼、发简历、回复 HR 都是**真实动作**，没有"演练模式"（`real` 参数本版本恒为 `True`）。

- **没有只读试跑** —— 不要为了验证链路而真的投递
- 界面上的「开始投递」点了就是真实投递

### 2. 本地文件含敏感数据，**绝不提交**

| 路径 | 含什么 |
|---|---|
| `run/config.json` | **LLM API Key**（以及端点、模型名） |
| `run/.job_hunter/` | **浏览器用户目录，含 BOSS 登录态 Cookie** |
| `run/resume.md` | **简历正文**（真实姓名、电话、工作经历） |
| `*.log` | 运行日志，可能含岗位与 HR 信息 |

这些已在 `.gitignore` 中排除。**提交前请务必跑一遍 `git status` 确认。**
详见 [`docs/PRIVACY.md`](docs/PRIVACY.md)。

### 3. 它接管的是你已经登录的浏览器

程序通过 CDP（默认 `9222` 端口）接管**你自己手动登录好**的 Chrome。

- 项目**不接管、不存储你的账号密码**
- 但被接管的浏览器对程序是**完全开放的** —— 请只在自己的机器上运行
- ⚠️ **不要**把 `9222` 端口暴露到公网 / 局域网（相当于把你的 BOSS 会话开放出去）

### 4. 第三方 LLM 会收到你的数据

岗位 JD、简历正文、HR 消息会被发送到你配置的 LLM 端点。

- 请使用你信任的服务商
- 注意该服务商的数据留存政策

---

## 如果密钥或账号泄露了

| 泄露内容 | 立即做什么 |
|---|---|
| **LLM API Key** | 到服务商后台**吊销并重新生成**，替换 `run/config.json` |
| **BOSS 登录态** | 在 BOSS 直聘**退出登录**（会让所有设备下线），并删除 `run/.job_hunter/` |
| **简历正文** | 删除 `run/resume.md` 及其 `.bak`，必要时重新上传 |
| **已误提交到 Git** | 见 [`docs/PRIVACY.md`](docs/PRIVACY.md) 的补救步骤（注意：仅删文件**不会**清除历史，需要重写历史或 Rotate 密钥） |

---

## 平台合规提醒

自动化操作第三方招聘平台可能违反其用户协议，存在**账号风控风险**。请自行评估，本项目不对使用后果负责（见 README 免责声明）。
